{
  "name": "compute-dynbackends-dev",
  "version": "1.0.0",
  "description": "Fastly Compute service with Dynamic Backends for proxying requests to arbitrary origins",
  "main": "src/index.js",
  "type": "module",
  "scripts": {
    "build": "js-compute-runtime src/index.js bin/main.wasm",
    "deploy": "fastly compute publish"
  },
  "devDependencies": {
    "@fastly/js-compute": "^3.25.0"
  }
}
